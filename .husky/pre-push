#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-push checks..."
echo "📋 Linting entire codebase..."
npm run lint

if [ $? -ne 0 ]; then
  echo "❌ Lint errors found! Fix them before pushing."
  exit 1
fi

echo "✅ All checks passed! Pushing..." 